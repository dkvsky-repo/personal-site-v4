// import { getData } from "./markdown-importer/markdownImporter.js";
// import { SOCIAL } from "../constants/resume.js";

// const getSocialNetworks = () => {
//   return getData(SOCIAL);
// };

// const data = getSocialNetworks();
// export { data };

// @todo: Turn this into a script that pulls the data from
// the "social" markdown file and converts it to "socialNetworksData.json".
// Then we would only need to run this script on build to keep data updated.
// The markdown file would be the source of truth.
